
export const metadata = {
    title: "LogIn for  Nomorede",
    description: "",
};

export default function RootLayout({ children }) {
    return (
        <> 
      
        {children}
        </>
    );
}
