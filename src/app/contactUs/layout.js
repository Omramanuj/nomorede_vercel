
export const metadata = {
    title: "contact Nomorede",
    description: "",
};

export default function RootLayout({ children }) {
    return (
        <> 
      
        {children}
        </>
    );
}
