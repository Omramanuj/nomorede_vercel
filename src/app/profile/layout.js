
export const metadata = {
    title: "Profile for  Nomorede",
    description: "",
};

export default function RootLayout({ children }) {
    return (
        <> 
      
        {children}
        </>
    );
}
